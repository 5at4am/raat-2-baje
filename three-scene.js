/* ------------------------------------------------------------------
   Raat 2 Baje — 3D particle layer
   The rain streaks and floating embers ("flies") effects have been
   removed. This file now only releases the entrance-animation gate
   (`.pre-anim`) so the UI fades in.
   ------------------------------------------------------------------ */
(function(){
  document.documentElement.classList.remove('pre-anim');
})();
